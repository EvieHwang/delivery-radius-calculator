# delivery-radius-calculator Environment Variables
#
# This file documents required environment variables.
# Copy to .env for local development (never commit .env!)
#
# IMPORTANT: In production, secrets are fetched from AWS Secrets Manager,
# not environment variables. These are only for local development.

# =============================================================================
# AWS Configuration
# =============================================================================

# AWS region for all services
AWS_REGION=us-west-2

# Secret name in AWS Secrets Manager (contains all app secrets)
SECRET_NAME=delivery-radius-calculator/prod

# =============================================================================
# Local Development Only
# =============================================================================
# These are only used when running locally. Production fetches from Secrets Manager.

# Anthropic API key for Claude (if using AI features)
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# GitHub Secrets (for CI/CD)
# =============================================================================
# These must be configured in GitHub repository settings:
#
# AWS_ACCESS_KEY_ID     - IAM user access key for deployments
# AWS_SECRET_ACCESS_KEY - IAM user secret key for deployments
# ANTHROPIC_API_KEY     - (optional) For tests that need Claude API

# =============================================================================
# AWS Secrets Manager Setup
# =============================================================================
# Create the secret in AWS Secrets Manager:
#
#   aws secretsmanager create-secret \
#     --name delivery-radius-calculator/prod \
#     --secret-string '{"ANTHROPIC_API_KEY": "sk-ant-..."}'
#
# Update a secret:
#
#   aws secretsmanager update-secret \
#     --secret-id delivery-radius-calculator/prod \
#     --secret-string '{"ANTHROPIC_API_KEY": "sk-ant-new-key"}'
#
# Secrets are automatically picked up on next Lambda cold start.
